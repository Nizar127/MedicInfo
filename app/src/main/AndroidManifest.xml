<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.doctor.medicinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/heart"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/heart"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicInfo"
        tools:replace="android:icon">
        <activity android:name=".Edit_patient_condition"></activity>
        <activity android:name=".Patient_condition.DayActivity" />
        <activity android:name=".Patient_condition.WeekActivitiy" />
        <activity android:name=".AdminSignUp" />
        <activity android:name=".StentDetail" />
        <activity android:name=".AddPatient" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".AdminAuthenticationActivity" />
        <activity android:name=".Patient_condition.HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>